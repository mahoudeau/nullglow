# Nullglow Neon for zsh. Generated by build.py. Don't edit it.
#
# Add ONE line to the end of your ~/.zshrc, after compinit and after the zsh
# plugins load:
#
#   source ~/.config/nullglow/theme.zsh
#
# That's the whole install. This file owns bat, LS_COLORS, fzf, syntax
# highlighting, autosuggestions, the completion menu and history search.
# Switching variants is re-running install.sh, not editing your rc.

# ── bat, and delta through it ────────────────────────────────────────────────
export BAT_THEME="Nullglow Neon"

# ── ls, eza and fd, through LS_COLORS ────────────────────────────────────────
if command -v vivid >/dev/null 2>&1; then
  export LS_COLORS="$(vivid generate nullglow-neon)"
fi

# ── fzf. Appended, so options you set earlier survive ────────────────────────
export FZF_DEFAULT_OPTS="${FZF_DEFAULT_OPTS:-} \
  --color=bg+:#0e1512,bg:-1,spinner:#05f98b,hl:#f81578 \
  --color=fg:#cfd8d3,header:#f81578,info:#c312f9,pointer:#05f98b \
  --color=marker:#05f98b,fg+:#cfd8d3,prompt:#05f98b,hl+:#f81578 \
  --color=border:#1c2723"

# ── zsh-syntax-highlighting ──────────────────────────────────────────────────
typeset -A ZSH_HIGHLIGHT_STYLES
ZSH_HIGHLIGHT_STYLES[command]='fg=#05f98b'
ZSH_HIGHLIGHT_STYLES[builtin]='fg=#05f98b'
ZSH_HIGHLIGHT_STYLES[function]='fg=#05f98b'
ZSH_HIGHLIGHT_STYLES[alias]='fg=#05f98b'
ZSH_HIGHLIGHT_STYLES[precommand]='fg=#05f98b,italic'
ZSH_HIGHLIGHT_STYLES[unknown-token]='fg=#fe1115,bold'
ZSH_HIGHLIGHT_STYLES[path]='fg=#0690fc,underline'
ZSH_HIGHLIGHT_STYLES[single-quoted-argument]='fg=#fcc60b'
ZSH_HIGHLIGHT_STYLES[double-quoted-argument]='fg=#fcc60b'
ZSH_HIGHLIGHT_STYLES[comment]='fg=#6e7d71,italic'
ZSH_HIGHLIGHT_STYLES[redirection]='fg=#c312f9'
ZSH_HIGHLIGHT_STYLES[reserved-word]='fg=#f81578'

# ── zsh-autosuggestions ──────────────────────────────────────────────────────
ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE='fg=#6e7d71'

# ── completion menu ──────────────────────────────────────────────────────────
# This one has to live here, not in your rc. It expands $LS_COLORS at the point
# it's called, so it has to run after the export above, not before it.
zstyle ':completion:*' list-colors "${(s.:.)LS_COLORS}"
zstyle ':completion:*:descriptions' format '%F{#c312f9}%B%d%b%f'
zstyle ':completion:*:messages'     format '%F{#0690fc}%d%f'
zstyle ':completion:*:warnings'     format '%F{#f81578}no match%f'
zstyle ':completion:*:corrections'  format '%F{#fcc60b}%d (errors: %e)%f'

# ── history-substring-search ─────────────────────────────────────────────────
HISTORY_SUBSTRING_SEARCH_HIGHLIGHT_FOUND='bg=#1c2723,fg=#fcc60b,bold'
HISTORY_SUBSTRING_SEARCH_HIGHLIGHT_NOT_FOUND='fg=#fe1115,bold'
